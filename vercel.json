{
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping dependency installation.'",
  "buildCommand": "mkdir dist && cp index.html index.tsx metadata.json types.ts dist/ && cp -r components services dist/"
}